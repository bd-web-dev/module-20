const pen = {
  brand: "Econo",
  price: 10,
  color: "Black",
};

const pencil = new Object();
console.log(pencil);

const rubber = Object.create({});
console.log(rubber);

// Using new keyword for class based objects
